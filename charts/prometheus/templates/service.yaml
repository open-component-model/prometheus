apiVersion: v1
kind: Service
metadata:
  name: prometheus
  labels:
    app: prometheus
spec:
  type: ClusterIP
  ports:
  - port: {{ default 8080 .Values.servicePort }}
    targetPort: {{ default 9090 .Values.containerPort }}
    name: default
  selector:
    app: prometheus
